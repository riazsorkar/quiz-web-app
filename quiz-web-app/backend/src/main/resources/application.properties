# backend/src/main/resources/application.properties
# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/quiz_app
spring.datasource.username=postgres
spring.datasource.password=1234

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080
server.error.include-message=always

# JWT Configuration
quiz.app.jwtSecret=quizAppSecretKeyForJWTTokenGenerationAndValidation2023
quiz.app.jwtExpirationMs=86400000

# Security
spring.security.filter.order=10

# Logging
logging.level.org.springframework.security=DEBUG

# CORS Configuration (we'll update this later)